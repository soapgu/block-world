# 贪吃蛇复刻开发步骤（React + TypeScript · 诺基亚经典风）

> 目标：用 **React + TypeScript** 复刻诺基亚 3310 时代的经典贪吃蛇，作为 `games/` 下的第二个正式工程。
> 致敬对象：诺基亚《贪吃蛇 II》（Snake II）——深绿 LCD 屏、方块蛇、像素食物、随长度逐步加速的紧张感。
> 本文档为**开发步骤稿**，按阶段推进，每阶段完成后试玩验收再进入下一阶段。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 游戏名（暂定） | 《方块世界 · 贪吃蛇》 |
| 类型 | 网格走位收集（单人） |
| 平台 | 网页（Vite + React + TypeScript 工程化构建） |
| 工程位置 | `games/snake/`（`package.json`、`src/` 均在此目录下） |
| 画面风格 | 诺基亚单色 LCD 风（对齐首页 `#041a04` 深绿底 + 亮绿像素，与 tetris 视觉统一） |
| 版本节奏 | v1 复古极简 → v2 现代规则 → v3 打磨（单一工程内按里程碑推进，git tag 标记） |

### 1.1 与 tetris 工程的复用关系

tetris（`games/tetris/`）已跑通全链路，snake **完整复制其工程模式**，以下经验直接继承、不再重复踩坑：

| 经验 | 来源 |
|---|---|
| `vite.config.ts` 用 `base: './'`（部署在 `/block-world/games/snake/` 二级子路径，写 `'/block-world/'` 会资源 404） | tetris 阶段 0 实测教训 |
| 分层架构：`src/game/` 纯逻辑（可单测）+ hooks 粘合 + canvas 管帧 + React 管框 | tetris 全程 |
| 引擎事件多播（`subscribe`）驱动音效/最高分，零引擎侵入 | tetris v3.1/v3.5 |
| 移动端触控与竖屏布局（`(pointer: coarse)` 检测、底部按键区 + safe-area、画布高度约束公式） | tetris v3.3 |
| 复古 LCD 皮肤（机身外壳/丝印螺丝/熄灭段残影/扫描线，零外部资源） | tetris v3.4 |
| 游戏循环 dt 钳制（切后台巨帧防护） | tetris useGameLoop |

### 1.2 诺基亚贪吃蛇要点（还原目标）

- 方块蛇：蛇身每节是一个实心方格，头部略亮/带描边区分
- 像素食物：闪烁的单一方格
- 逐步加速：吃得越多爬得越快（诺基亚手感核心）
- 死亡判定：撞墙、撞自己（诺基亚默认撞墙死，不做穿墙模式）

---

## 2. 技术选型与目录结构

### 2.1 技术栈（与 tetris 一致）

Vite + React 18 + TypeScript（strict）+ Vitest；游戏区 `<canvas>` 高频重绘，React 只管外壳 UI（分数/状态/遮罩）；npm 管理依赖。

### 2.2 工程目录结构（目标形态）

```
games/snake/
├── index.html              # viewport-fit=cover
├── package.json / tsconfig.json / vite.config.ts（base: './'）
├── src/
│   ├── main.tsx / App.tsx  # 外壳布局 + 事件接线
│   ├── game/
│   │   ├── types.ts        # Point、Dir、SnakeState、Stats
│   │   ├── grid.ts         # 网格常量（如 20×20）、点在界内/碰撞纯函数
│   │   ├── snake.ts        # 蛇数据模型（坐标数组）、移动、吃食增长、自撞判定
│   │   ├── food.ts         # 食物生成（避开蛇身）、特殊食物（v2）
│   │   ├── engine.ts       # 状态机 ready/playing/paused/over、定时步进、计分/加速、GameEvent 事件
│   │   └── tuning.ts       # TUNING 常量（初速、加速曲线、计分表）
│   ├── hooks/
│   │   ├── useGameLoop.ts  # rAF + dt 钳制（可从 tetris 复制）
│   │   ├── useKeyboard.ts  # 方向键 + 方向队列（防 180° 回头、防连按丢输入）
│   │   └── useAutoPause.ts # 失焦自动暂停（v2）
│   ├── render/
│   │   └── canvas.ts       # 画网格、蛇（头/身区分）、食物闪烁、熄灭段残影
│   ├── audio/              # v3：context.ts + sfx.ts（复用 tetris 设计）
│   └── components/
│       ├── BoardCanvas.tsx / StatsPanel.tsx / Overlay.tsx
│       ├── CompactBar.tsx   # 移动端精简信息条（v3）
│       └── TouchControls.tsx # 移动端虚拟方向键（v3，十字布局）
└── tests/
    ├── snake.test.ts / food.test.ts / engine.test.ts
```

**分层原则**与 tetris 相同：`src/game/` 不碰 DOM/React，独立单测；React 层只做"输入 → 引擎 → 渲染"粘合。

### 2.3 核心数据模型（关键设计）

```ts
// 蛇：头在前、尾在后的坐标数组；移动 = 头部入列 + 尾部出列（吃食时尾部不出列）
type SnakeBody = Point[]  // [{x, y}, ...]，snake[0] 为头
type Dir = 'up' | 'down' | 'left' | 'right'

// 方向队列：缓存最近 2 次按键，每步只消费 1 个，
// 解决"快速按 ↑← 导致回头自杀"的经典 bug
```

---

## 3. 部署方案（复用现有链路）

**不新增 workflow**。现有 `.github/workflows/deploy.yml`（rsync 反向排除组装全站 + deploy-pages）只需两处小改：

1. 构建步骤改为依次构建 `games/tetris` 与 `games/snake`（或拆成两个 step）
2. 组装产物时加一行：`cp -a games/snake/dist/. _site/games/snake/`

首页链接 `games/snake/` 自然衔接；`.gitignore` 已忽略 `node_modules/`、`dist/`（工程内自带）。

## 4. 首页导航入口

`index.html` 的 `.page` 容器内新增第三张卡片（复用 `.card` 样式）：

```html
<a class="card" href="games/snake/">
  <span class="tag">GAME · V1</span>
  贪吃蛇
</a>
```

---

## 5. 分阶段开发步骤

### 阶段 0：工程与部署链路（半步，比 tetris 轻得多）

| # | 任务 | 产出 |
|---|---|---|
| 0.1 | 手写 `games/snake/` 最小工程（复制 tetris 模式：package.json/tsconfig/vite.config/index.html/src 占位页），`npm install` 出 lock 文件 | 本地可 dev 可 build |
| 0.2 | `deploy.yml` 加 snake 构建 + 产物拷贝 | 推送即部署 |
| 0.3 | 首页加导航卡片 | 线上可点进占位页 |

**验收**：Actions 绿、tetris 与拳击旧页面不受影响、`games/snake/` 占位页可访问。

### 阶段 v1：复古极简（核心可玩，诺基亚手感）

| # | 任务 | 要点 |
|---|---|---|
| 1.1 | `types.ts` + `grid.ts` | 20×20 网格；`insideGrid(p)`、蛇身/食物碰撞纯函数 |
| 1.2 | `snake.ts` | 坐标数组模型；`step(snake, dir)` 移动；吃食增长；`hitsSelf` 判定 |
| 1.3 | `food.ts` | 随机生成且**永不落在蛇身上**；场上始终恰好 1 个 |
| 1.4 | `engine.ts` | 状态机 + 定时步进（间隔随分数加速：初始 200ms，每吃 1 个 -4ms，下限 70ms）+ 计分（每食 10 分）+ GameEvent（eat/over 等） |
| 1.5 | `useKeyboard.ts` | 方向键/WASD；**方向队列**（长 2）防回头与丢输入；回车开始/重开 |
| 1.6 | `useGameLoop.ts` + `canvas.ts` | rAF 主循环 + dt 钳制；画网格、蛇（头部亮绿描边、身体暗绿）、食物闪烁 |
| 1.7 | `StatsPanel.tsx` + `Overlay.tsx` | 分数/长度面板；开始/结束遮罩（对齐 tetris 布局，UI 快照浅比较同步） |
| 1.8 | 手感调参 | TUNING 集中调：初速、加速曲线、网格尺寸 |

**单元测试**：`snake.test.ts`（移动/增长/自撞/回头拒绝）、`food.test.ts`（生成不在蛇身、棋盘将满时的行为）、`engine.test.ts`（吃食计分、加速、死亡结束、事件序列）。

**验收**：完整玩一局——方向响应跟手、快速转向不误死、吃食变长加速、撞墙/撞己结束、回车重开。

### 阶段 v2：现代规则（从"能玩"到"好玩"）

| # | 任务 | 要点 |
|---|---|---|
| 2.1 | 特殊食物 | 每吃 5 个普通食物出现一次限时高分食物（~5s 消失、闪烁更快、50 分） |
| 2.2 | 暂停/继续 | P 键 + 失焦自动暂停（复用 useAutoPause） |
| 2.3 | 本地最高分 | BEST 栏 + 新纪录高亮（完整复用 tetris useBestScore 模式） |

**验收**：特殊食物限时消失且分数翻倍；暂停/继续正确；最高分持久化。

### 阶段 v3：打磨与氛围（从"好玩"到"有味"）

| # | 任务 | 要点 |
|---|---|---|
| 3.1 | 合成音效 | 吃食短哔、特殊食物双哔、死亡下行音；共享 AudioContext + M 静音（复用 tetris audio 模块设计，代码可搬） |
| 3.2 | 移动端适配 | 虚拟**十字方向键**（触屏专用：上/左/下/右 一簇 + 暂停收顶部工具栏）；画布高度约束公式复用 tetris 验证过的 `100dvh - 常量` 方案 |
| 3.3 | 诺基亚 LCD 皮肤 | 机身外壳 + 丝印"NOKIA 风"装饰字 + 熄灭段残影 + 扫描线（tetris v3.4 方案直接复用） |
| 3.4 | 死亡回放细节 | 死亡时蛇身逐节熄灭的短动画（~0.5s）再出结算 |

**验收**：手机竖屏完整可玩（十字键跟手、整页不滚动）；音效开关记忆；皮肤上线。

### v3 之后：可选彩蛋（不承诺）

- 「1975 模式」：致敬贪吃蛇起源 Atari Blockade——无食物、纯比拼生存的策略模式
- slither.io 风格的平滑移动/惯性转向变体（工作量大，仅列方向）

---

## 6. 测试与验收总览

| 层 | 手段 | 覆盖 |
|---|---|---|
| 纯逻辑 | Vitest 单测 | 蛇移动/增长/自撞、食物生成约束、计分/加速、事件序列 |
| 界面/手感 | 人工试玩清单 | 每阶段文末"验收"项（重点：方向队列手感、加速曲线、移动端十字键） |
| 部署 | Actions 产物检查 | tetris/拳击/首页零损坏，snake 入口可达 |

CI：deploy.yml 已有 `npm test` 前置，snake 接入后测试不过不部署。

---

## 7. 里程碑与提交规划

| 里程碑 | 内容 | git tag | 提交类型（git-cz） |
|---|---|---|---|
| M0 | 工程 + 部署 + 首页入口 | `snake-v0` | `chore(snake)` 脚手架、`chore(ci)` deploy 扩展、`feat(web)` 首页卡片 |
| M1 | v1 复古极简可玩 | `snake-v1` | `feat(snake)` 按模块分次提交 |
| M2 | v2 现代规则 | `snake-v2` | `feat(snake)` 特殊食物/暂停/最高分 |
| M3 | v3 打磨发布 | `snake-v3` | `feat(snake)` 音效/移动端/皮肤 |

每阶段完成后试玩反馈，调 TUNING 再进下一阶段——与 tetris 迭代方式一致。

---

## 8. 风险与注意

| 风险 | 影响 | 对策 |
|---|---|---|
| deploy.yml 多工程构建变慢 | 部署时间上升 | 两个工程构建步骤并行（jobs 或 `&`），或接受分钟级 |
| 方向队列处理不当 | 快速转向误死/丢输入 | 队列长度 2 + 每步只消费 1 个 + 单测覆盖连续转向用例 |
| 移动端十字键误触 | 体验差 | 按键间距与 62px 尺寸沿用 tetris 验证值；方向键单击即转向（无需长按重复） |
| 与 tetris 共享部署产物 | 相互覆盖 | 组装脚本逐工程显式拷贝（`_site/games/<name>/`），互不干扰 |
| dt 巨帧 | 切后台回来蛇瞬移穿墙/暴毙 | useGameLoop dt 钳制 100ms（tetris 同款） |
