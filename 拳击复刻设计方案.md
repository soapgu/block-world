# 俯视拳击复刻设计方案（机器人拳击 · 致敬版）

> 目标：复刻 90 年代国产掌机（俄罗斯方块机）内置的「拳击对战」（俗称「机器人拳击」）小游戏。
> 由于原版资料极少（无截图、无 ROM、模拟器未收录），本方案**基于用户记忆 + 已确认的共性**进行还原设计，先出可玩原型，再按记忆逐项迭代。
> 本文档为**设计稿**，确认后再写代码。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 游戏名（暂定） | 《拳击对战 · 机器人版》 |
| 类型 | 回合制出拳对打（单人 vs 电脑） |
| 平台 | 网页（HTML + CSS + JS 单文件，浏览器直接打开） |
| 画面风格 | 单色点阵 LCD 风（深色底 + 亮色像素，模仿当年绿/蓝屏） |
| 核心体验 | 「操作简单但节奏感强」——看准时机出拳、抓对方破绽命中 |
| 版本 | v1 可玩原型 → v2 打磨 → v3 加味 |

**设计依据（已确认的事实）**：
- 国产掌机确实内置「拳击对战：两个像素小人对打，操作简单但节奏感强」（搜狐《掌心的方块宇宙》）
- 被归为「画风过于抽象」的山寨游戏（游研社）
- 您的记忆：**俯视/顶视、只能看到头顶、6 格方块拳击手（2 格手 + 4 格身体）、格子血条、画面简陋**

---

## 2. 画面设计（俯视 · 6 格方块拳击手）

### 2.1 整体布局（点阵屏模拟）

```
┌────────────────────────────────────────┐
│  [电脑] ■■■■■                   05:00  │   ← 顶部：电脑生命格子 + 倒计时
│                                        │
│        ┌──────────────────┐            │
│        │     电脑(俯视)     │            │
│        │    ◇  ◇          │            │   ← 电脑拳击手（朝下）
│        │   [□□]           │            │
│        │   [□□]           │            │
│        │                  │            │
│        │                  │            │
│        │   [□□]           │            │
│        │   [□□]           │            │   ← 玩家拳击手（朝上）
│        │    ◇  ◇          │            │
│        │     玩家(俯视)     │            │
│        └──────────────────┘            │
│                                        │
│  [玩家] ■■■■■                   05:00  │   ← 底部：玩家生命格子 + 倒计时
│            ← → 出拳                      │   ← 操作提示
└────────────────────────────────────────┘
```

### 2.2 拳击手：6 格方块（2 格手 + 4 格身体）

从头顶俯视，每个拳击手由 **6 个像素格**构成：

- **身体**：4 格（2×2），代表头顶 + 躯干
- **手**：2 格（身体前方左右各 1 格），出拳时向前伸

**玩家（朝上，面向电脑）**：

```
      ◇ ◇        ← 双手（左拳、右拳各 1 格，位于前方）
    [ □□ ]       ← 身体 4 格（2×2，俯视看到的是「头顶+肩」）
    [ □□ ]
```

**电脑（朝下，面向玩家）**：镜像即可。

**出拳表现**：按左拳键 → 左手格向前（上）伸出 2~3 格距离再收回；右拳同理。伸出的手就是"拳头"。

### 2.3 颜色与质感（LCD 风）

| 元素 | 表现 |
|---|---|
| 背景 | 深墨绿/深蓝（模拟液晶底色） |
| 像素 | 亮绿/亮蓝（模拟液晶点亮段） |
| 拳击手 | 亮色 6 格方块，电脑/玩家用不同色（如玩家亮绿、电脑暗绿） |
| 生命格子 | 5 个方块：■ 剩余 / □ 已掉，排列在双方名字旁 |
| 噪点/网格 | 可加轻微网格线增强点阵感 |

---

## 3. 玩法规则（核心）

**选定方案：单人打电脑 · 只有出拳 · 各 5 格生命**

### 3.1 基础规则

1. 双方各 **5 格生命**，站在擂台两端（玩家在下、电脑在上）。
2. 玩家按 **← / → 出拳**（左拳/右拳），电脑自动出拳。
3. 出拳打中对方 → 对方 **掉 1 格生命**。
4. 一方生命先打空 → **KO**，对方获胜。
5. 单局不限时（可选加 60 秒倒计时，时间到比剩余格数，平局再来）。
6. **身体不能互相穿过**：两个拳击手之间始终保持最小间距（约 40px）——靠近到极限会被互相挡开，谁也不能挤进对方的身体；打中的唯一方式是**拳头伸出去够到对方身体**（对应实现中的 `MIN_GAP` / `clampGap`）。

### 3.2 出拳机制（时机博弈，制造"节奏感"）

出拳不是瞬间命中，而是有一个完整动作周期，让玩家需要**抓时机**：

| 阶段 | 时长 | 说明 |
|---|---|---|
| 前摇（抬手） | ~0.25s | 手抬起准备，此时**不产生命中** |
| 命中窗口 | ~0.15s | 手伸出到达攻击距离，**命中判定在此阶段** |
| 收拳 | ~0.30s | 手收回，此阶段**不能再次出拳** |
| 冷却 | ~0.20s | 完全可再次出拳 |

完整一拳约 **0.9s**，期间无法再次出拳。

**命中判定（关键）**：
- 先进入「命中窗口」的一方先命中 → 对方掉 1 格，且**对方的出拳被打断**（如果对方也在出拳）。
- 双方同时进入命中窗口 → **互换**（各掉 1 格）。
- 所以策略 = **别在对方抬手时出拳（会被反打）**，抓**对方收拳硬直**时出拳必定命中。

> 这套机制把当年"按键出拳对打"还原成有节奏感的时机博弈：看得准、点得稳。

### 3.3 操作

| 按键 | 动作 |
|---|---|
| ← | 出左拳 |
| → | 出右拳 |
| （可选）空格 / ↑ | 出拳（随机左右手） |
| 回车 | 开始 / 再来一局 |

无躲避、无格挡（按您的选择保持最简单）。

---

## 4. AI 设计（电脑对手）

电脑目标：让玩家觉得"像个人在对打"，有节奏、有破绽、能反打。

| 参数 | 说明 | 取值 |
|---|---|---|
| 出拳间隔 | 每 0.8~1.6s 随机决定是否出拳 | 概率 ~55% |
| 出拳前摇 | 与玩家相同（0.25s），公平 | 固定 |
| 反打反应 | 玩家出拳后，电脑有小概率立即反击 | 简单 10% / 普通 25% / 困难 45% |
| 收拳破绽 | 电脑收拳后停 0.5s 左右（留给玩家抓窗口） | 随机 0.3~0.7s |

**难度分级**（开局选择，或按连续获胜自动升级）：
- **简单**：电脑出拳慢、破绽大（出拳间隔 1.2~2.0s）
- **普通**：默认节奏（0.8~1.6s）
- **困难**：出拳快、爱反打（0.5~1.2s，反打 45%）

---

## 5. 游戏流程

```
标题画面（游戏名 + 点阵风格 LOGO）
   ↓ 按回车
难度选择（简单 / 普通 / 困难）← 可选，v1 可先固定普通
   ↓
战斗画面（双方就位 → "3、2、1、开始！" 或直接开始）
   ↓
对打（出拳 / 命中 / 掉血 / 音效哔声）
   ↓ 一方 5 格打空
KO 结算（大字 "KO！你赢了 / 你输了"，点阵闪烁）
   ↓ 按回车
再来一局 / 返回标题
```

---

## 6. 数据与代码结构（为 v1 实现准备）

建议单文件 `boxing.html`，含 HTML+CSS+JS。

```js
// 拳击手对象
class Boxer {
  constructor(side) {        // side: 'player' | 'enemy'
    this.x, this.y;          // 在擂台中的位置
    this.hp = 5;             // 生命 5 格
    this.facing = side==='player' ? -1 : 1;  // 朝向
    this.hand = 'left';      // 当前出哪只手
    this.punchState = null;  // null | 'windup' | 'strike' | 'recover'
    this.punchTimer = 0;     // 出拳阶段计时
    this.cooldown = 0;       // 冷却计时
  }
  canPunch() { return this.punchState===null && this.cooldown<=0; }
  punch()    { /* 进入 windup，播前摇 */ }
  update(dt) { /* 推进阶段机：windup→strike→recover→null */ }
  render(ctx){ /* 画 6 格方块：4 格身体 + 2 格手，手按阶段伸出 */ }
}

// 游戏主循环
class Game {
  player, enemy;       // 两个 Boxer
  state;               // 'title'|'fight'|'ko'
  timer;               // 倒计时（可选）
  update(dt) {
    // 1. 更新双方出拳状态
    // 2. 命中判定：比较双方进入 strike 的时刻
    // 3. 掉血 / KO 判定
    // 4. AI：控制 enemy 按节奏出拳
  }
  render() { /* 画擂台、双方、生命格子、倒计时 */ }
}
```

**命中判定伪代码**：
```
当某一方进入 strike 时刻 t：
  若对方不在 strike 中：
    对方掉 1 格，对方的出拳被打断 → 进入 recover
  若对方同时在 strike：
    双方各掉 1 格（互换）
  若一方 hp <= 0：进入 KO 结算
```

**渲染核心**（画拳击手）：
```
绘制身体 2×2（4 格）
绘制双手 2 格（基础位：身体前方左右）
若 punchState==='strike'：将对应手前伸 2~3 格（绘制为加长拳头）
```

---

## 7. 迭代计划

| 版本 | 内容 |
|---|---|
| **v1 可玩原型** | 俯视画面 + 6 格方块拳击手 + 5 格生命 + 出拳时机博弈 + 普通难度 AI + 开始/KO 流程 |
| **v2 打磨** | 简单/普通/困难三档 AI、倒计时模式、命中"哔"声、KO 特效闪烁 |
| **v3 加味** | LCD 噪点/网格质感、复古边框、"货郎"BGM（可选）、连续获胜难度升级 |

每版做完请您试玩，按记忆反馈调整：画面像不像、节奏快不快、血量对不对、拳头伸得够不够远。

---

## 8. 与原版记忆的对照（诚实说明）

| 记忆/事实 | 本方案如何还原 | 依据 |
|---|---|---|
| 俯视/顶视、只能看到头顶 | ✅ 俯视视角，拳击手为 2×2 身体 + 2 手 6 格 | 您的记忆 |
| 6 格方块（2 手 + 4 身） | ✅ 精确按 6 格设计 | 您的记忆 |
| 画风抽象、简陋 | ✅ 单色点阵 LCD 风，极简像素 | 游研社「画风过于抽象」 |
| 格子血条 | ✅ 5 格生命格，打中掉一格 | 您的记忆 + 各掌机拳击共性 |
| 单人打电脑 | ✅ 单人 vs AI | 您的选择 |
| 只有出拳、操作简单 | ✅ 仅 ←/→ 出拳，时机博弈 | 您的选择 + 搜狐「操作简单但节奏感强」 |
| 原版具体操作/节奏/颜色 | ⚠️ **无法考证**，为合理推断 | 原版无截图/ROM/模拟器 |

> 说明：本方案是"致敬/还原版"，在无法获得原版资料的前提下，先还原您记忆中的**画面与核心手感**，具体细节（拳头伸出长度、出拳节奏、音效等）通过迭代对您的主观回忆校准。
