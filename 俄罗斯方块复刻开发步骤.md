# 俄罗斯方块复刻开发步骤（React + TypeScript · 工程版）

> 目标：用 **React + TypeScript** 复刻经典俄罗斯方块（90 年代方块掌机 Brick Game 的核心体验），作为仓库第一个**正式工程**（区别于 `prototype/` 下的单文件原型）。
> 玩法考证与变种调研见《俄罗斯方块调研报告.md》，本文档只管"怎么做"，不重复"考什么"。
> 本文档为**开发步骤稿**，按阶段推进，每阶段完成后试玩验收再进入下一阶段。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 游戏名（暂定） | 《方块世界 · 俄罗斯方块》 |
| 类型 | 经典落块消除（单人） |
| 平台 | 网页（Vite + React + TypeScript 工程化构建） |
| 工程位置 | `games/tetris/`（`package.json`、`src/` 均在此目录下） |
| 画面风格 | 复古单色点阵 LCD 风（对齐首页 `#041a04` 深绿底 + 亮绿像素） |
| 版本节奏 | v1 复古极简 → v2 现代经典规则 → v3 打磨（单一工程内按里程碑推进） |

### 1.1 目录定位：prototype/ 与 games/

| 目录 | 定位 | 形态 |
|---|---|---|
| `prototype/` | 快速原型：验证想法、随时可扔 | 单文件 HTML（vanilla JS），浏览器直接打开，无构建 |
| `games/` | 正式开发：可长期维护的游戏工程 | Vite + React + TS，有构建链、有测试、经 CI 部署 |

本次俄罗斯方块是**正式开发**，放 `games/tetris/`。boxer 若将来出正式版，可迁入 `games/boxing/`（不在本文档范围）。

### 1.2 与 boxer 原型的差异

| | boxer v1（原型） | tetris（正式工程） |
|---|---|---|
| 代码形态 | 单 HTML 文件 + 内联 script | Vite 项目，模块化 TS |
| 版本管理 | v1/v2/v3 各自独立目录 | **单一工程**，v1/v2/v3 是同一代码上的里程碑（git tag 标记），避免三份代码重复维护 |
| 运行方式 | 双击打开 | `npm run dev` 本地开发，Actions 构建部署 |
| 测试 | 无 | Vitest 单元测试关键逻辑 |

---

## 2. 技术选型与目录结构

### 2.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 构建 | Vite | 快、配置少，产物即纯静态文件 |
| 框架 | React 18 + TypeScript（strict） | 用户要求 |
| 渲染 | 游戏区 `<canvas>` + 2D context | 高频重绘交给 canvas（延续 boxer v1 做法）；React 只管外壳 UI |
| 状态 | useState/useRef + 自定义 hook | 游戏循环数据放 ref 避免每帧触发 React 渲染；分数等低频状态才进 state |
| 测试 | Vitest | 纯逻辑单测（无 DOM 依赖） |
| 包管理 | npm | 仓库首个工程，从简 |

### 2.2 职责划分（关键决策）

**Canvas 管帧，React 管框**：

- `<canvas>`：10×20 棋盘、当前方块、（v2 起）ghost/next/hold 的绘制，requestAnimationFrame 循环驱动。
- React 组件：分数/等级/消行数面板、开始/暂停/结束界面、操作说明，通过回调与游戏核心通信。

### 2.3 工程目录结构（目标形态）

```
games/tetris/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts          # base: '/block-world/'
├── src/
│   ├── main.tsx            # 入口
│   ├── App.tsx             # 外壳布局（棋盘 + 面板 + 遮罩层）
│   ├── game/
│   │   ├── board.ts        # 棋盘数据模型：10×20 网格、落子、消行检测
│   │   ├── pieces.ts       # 7 种方块定义（I O T S Z J L）+ 旋转矩阵
│   │   ├── engine.ts       # 游戏核心：状态机、重力、锁定、计分、等级
│   │   ├── randomizer.ts   # 发牌器（v1 纯随机 → v2 7-bag）
│   │   └── types.ts        # 共享类型（Cell、Piece、GameState…）
│   ├── hooks/
│   │   ├── useGameLoop.ts  # requestAnimationFrame + 固定步长更新
│   │   └── useKeyboard.ts  # 按键映射 + DAS 自动重复移位（左右长按）
│   ├── render/
│   │   └── canvas.ts       # canvas 绘制：格子、方块、网格线
│   └── components/
│       ├── BoardCanvas.tsx # canvas 容器组件
│       ├── StatsPanel.tsx  # 分数/等级/消行面板
│       └── Overlay.tsx     # 开始/暂停/GameOver 遮罩
└── tests/
    ├── board.test.ts
    ├── pieces.test.ts
    └── randomizer.test.ts
```

**分层原则**：`src/game/` 下的模块是**纯函数/纯类，不碰 DOM 和 React**，可以独立单测；React 层只做"输入 → 引擎 → 渲染"的粘合。

---

## 3. 部署方案（Vite + GitHub Actions）

仓库目前是 GitHub Pages **直接托管 main 分支根目录**的纯静态站。引入构建链后，Pages 来源需切换为 **GitHub Actions**，且部署产物必须包含旧页面，否则现有链接会 404。

### 3.1 构建产物路径约定

tetris 构建后部署到 `https://soapgu.github.io/block-world/games/tetris/`，与首页相对链接 `games/tetris/` 自然衔接：

```ts
// games/tetris/vite.config.ts
export default defineConfig({
  base: '/block-world/',   // 项目页子路径，必须设置
  plugins: [react()],
})
```

### 3.2 工作流设计（.github/workflows/deploy.yml）

```
触发：push 到 main
步骤：
  1. checkout
  2. setup-node（带 npm 缓存）
  3. cd games/tetris && npm ci && npm run build
  4. 组装部署目录 _site/：
     a. 拷贝仓库静态内容 → index.html、prototype/、course/、*.md 等
     b. 拷贝 games/tetris/dist/ → _site/games/tetris/
  5. actions/upload-pages-artifact + actions/deploy-pages
```

**关键点**：第 4a 步不能漏——Pages 切到 Actions 来源后，部署的只有 workflow 产出的 artifact，仓库根目录不再被直接托管。静态文件清单要在 workflow 里显式维护（或反向排除 `games/`、`.github/` 后全量拷贝，推荐后者）。

### 3.3 仓库设置（一次性手动操作）

Settings → Pages → Source 从 `Deploy from a branch` 改为 `GitHub Actions`。

### 3.4 过渡期验证清单

- [ ] 旧链接 `prototype/boxer/v1/index.html` 在 Actions 部署后仍可访问
- [ ] 首页 `index.html` 正常
- [ ] `games/tetris/` 打开为游戏入口，刷新子路由不 404（单页应用无路由则无此问题）

---

## 4. 首页导航入口

在 `index.html` 的 `.page` 容器内、boxer 卡片旁新增卡片（复用现有 `.card` 内联样式，无需改 CSS）：

```html
<a class="card" href="games/tetris/">
  <span class="tag">GAME · V1</span>
  俄罗斯方块
</a>
```

tag 用 `GAME` 区别于 `PROTOTYPE`，呼应"正式开发"的定位。

---

## 5. 分阶段开发步骤

### 阶段 0：工程与部署链路跑通（先修路，后造车）

| # | 任务 | 产出 |
|---|---|---|
| 0.1 | 在 `games/tetris/` 用 Vite 模板创建 React + TS 工程（`npm create vite@latest tetris -- --template react-ts`），开 strict | 可 dev 可 build 的空工程 |
| 0.2 | `vite.config.ts` 设 `base: '/block-world/'` | 本地 preview 路径正确 |
| 0.3 | 写 `.github/workflows/deploy.yml`（含静态文件组装），仓库 Pages 来源切到 Actions | 推送 main 即自动部署 |
| 0.4 | 首页加导航卡片（见第 4 节） | 线上可点进游戏占位页 |
| 0.5 | `.gitignore` 补 `node_modules/`、`dist/`；README 补一段工程说明 | 仓库卫生 |

**验收**：push 后 Actions 绿、旧页面全部可访问、首页新卡片能点进 tetris 占位页。

### 阶段 v1：复古 Brick Game 极简版（核心可玩）

按"数据 → 逻辑 → 渲染 → 输入 → 循环"顺序实现，每步可独立验证：

| # | 任务 | 要点 |
|---|---|---|
| 1.1 | `types.ts` + `board.ts` | 10×20 网格用 `(row, col)` 二维数组；`collides(board, piece)` 碰撞检测（越界/叠块）；`lockPiece` 固定落块入盘；`clearLines` 返回被消除的行号 |
| 1.2 | `pieces.ts` | 7 种方块用 4×4 或各自尺寸的坐标矩阵表示，旋转用矩阵变换（v1 简单旋转即可，不做踢墙） |
| 1.3 | `randomizer.ts` | v1 纯随机发牌（每块等概率），接口先按 7-bag 预留 |
| 1.4 | `engine.ts` 游戏状态机 | `ready / playing / paused / over`；重力按计时下落；到底锁定 → 消行 → 发新牌；堆到出生点即 GameOver |
| 1.5 | `canvas.ts` + `BoardCanvas.tsx` | 画棋盘网格、已固定块、当前块；格子用亮绿 + 深绿描边，对齐 LCD 风 |
| 1.6 | `useKeyboard.ts` | ←/→ 移动、↑ 旋转、↓ 软降、空格硬降、P 暂停；先做单次触发，长按重复（DAS）放 v1 末尾 |
| 1.7 | `useGameLoop.ts` | requestAnimationFrame + dt 累积；重力间隔由等级决定 |
| 1.8 | `engine.ts` 计分与等级 | 消行计分（1/2/3/4 行 = 100/300/500/800 × 等级）；每消 10 行升 1 级，下落加速 |
| 1.9 | `StatsPanel.tsx` + `Overlay.tsx` | 分数/等级/消行面板；开始/暂停/结束遮罩，回车重开 |
| 1.10 | 手感调参 | 下落初速、软降速度、硬降是否留消行停顿，试玩校准 |

**单元测试**（随实现同步写）：`board.test.ts`（碰撞边界、消行各种情况含四消、锁定越界判负）、`pieces.test.ts`（旋转后坐标正确）。

**验收**：能完整玩一局——移动/旋转/软硬降都响应、消行加分、加速升阶、堆满结束、回车重开。

### 阶段 v2：现代经典规则（从"能玩"到"好玩"）

| # | 任务 | 要点 |
|---|---|---|
| 2.1 | 7-bag 随机发牌 | 每 7 块一组洗牌发完再换组，杜绝久等 I 块；`randomizer.test.ts` 验证每 7 次恰好集齐 7 种 |
| 2.2 | Next 预览 | 队列展示接下来 1~3 块，小 canvas 或 CSS grid 绘制 |
| 2.3 | Hold 暂存 | Shift/C 键暂存当前块，每次锁定前只能换一次 |
| 2.4 | Ghost 幻影 | 当前方块的落点投影，暗色描边绘制 |
| 2.5 | SRS 旋转系统 + 踢墙 | 标准 Super Rotation System 偏移表（含 I 块特表）；`pieces.test.ts` 补踢墙用例 |
| 2.6 | 锁定延迟（lock delay） | 到底后 0.5s 才锁定，移动/旋转刷新延迟（设刷新上限 15 次，防无限拖延） |
| 2.7 | 消行动画 | 被消行闪烁/收缩 ~0.3s 再塌落，配短停顿增强打击感 |
| 2.8 | 计分补全 | 软降/硬降奖励（1 格 1 分 / 2 分）、T-Spin 可选（标记为 v3 后彩蛋亦可） |

**验收**：对照现代方块（如 Tetr.io / 官方 Tetris）的手感清单逐项试：7-bag 分布、踢墙转身、锁定延迟救块、ghost 对位都符合直觉。

### 阶段 v3：打磨与氛围（从"好玩"到"有味"）

| # | 任务 | 要点 |
|---|---|---|
| 3.1 | 音效 | 移动嘀、旋转嗒、锁定咚、消行哗、四消/Tetris 特效音；Web Audio 合成（不引音频文件，仓库保持轻量） |
| 3.2 | BGM | 复古循环旋律（可选《Korobeiniki》8-bit 改编），默认关闭、可开关 |
| 3.3 | 移动端适配 | 触控按钮（左右/旋转/软降/硬降/hold）、canvas 自适应缩放、禁止双击缩放 |
| 3.4 | 复古皮肤 | LCD 噪点/网格质感、像素字体、扫描线效果，对齐仓库 #041a04 主题；boxer v3 预留的"复古边框"可同款复用 |
| 3.5 | 本地最高分 | localStorage 记录 best score |
| 3.6 | 暂停/重开体验完善 | 失焦自动暂停、结束画面统计（PPS 等可选） |

**验收**：手机浏览器可顺畅游玩；音效开关状态记忆正确；最高分持久化。

### v3 之后：可选彩蛋方向（源自调研报告，不承诺）

- X 十字五连块、穿墙小点等国产掌机特殊变种（《俄罗斯方块调研报告.md》已考证），做成"掌机彩蛋模式"开关。
- 消除动画主题皮肤（复古掌机绿 / 灰白 Game Boy 双主题切换）。

---

## 6. 测试与验收总览

| 层 | 手段 | 覆盖 |
|---|---|---|
| 纯逻辑 | Vitest 单测 | 消行、碰撞、旋转/踢墙、7-bag、计分、等级加速 |
| 界面/手感 | 人工试玩清单 | 每阶段文末"验收"项，试玩通过才进下一阶段 |
| 部署 | Actions 产物检查 | 旧链接可访问、新入口可达（阶段 0 清单） |

CI 建议：workflow 中 `npm run build` 前跑 `npm test`，测试不过不部署。

---

## 7. 里程碑与提交规划

| 里程碑 | 内容 | git tag 建议 | 提交类型（git-cz） |
|---|---|---|---|
| M0 | 工程搭建 + 部署链路 + 首页入口 | `tetris-v0` | `chore(tetris)` 脚手架、`feat(web)` 首页导航、`chore(ci)` workflow |
| M1 | v1 复古极简可玩 | `tetris-v1` | `feat(tetris)` 按模块分次提交（board/pieces/engine/render…） |
| M2 | v2 现代规则 | `tetris-v2` | `feat(tetris)` 7-bag/SRS/hold…，`test(tetris)` 补用例 |
| M3 | v3 打磨发布 | `tetris-v3` | `feat(tetris)` 音效/适配/皮肤，`docs` 更新 README |

每阶段完成后请试玩反馈，按手感调整参数再进入下一阶段——与 boxer 的迭代方式一致。

---

## 8. 风险与注意（诚实说明）

| 风险 | 影响 | 对策 |
|---|---|---|
| Pages 切 Actions 来源后旧文件遗漏 | 现有页面 404 | 阶段 0 单独验证部署清单（3.4 节），先发一个只含"组装静态文件"的 workflow 再叠加构建 |
| React 每帧 setState 导致卡顿 | 帧率下降 | 游戏数据全走 ref + canvas 渲染，React 状态仅低频 UI；这是 2.2 节分层的原因 |
| DAS/锁定延迟等手感参数调不好 | "玩起来不像" | 参数集中成常量对象 `TUNING`，便于试玩后快速调参 |
| 仓库首次引入 node 工程链 | 仓库体积/协作成本 | `node_modules`、`dist` 严格 gitignore；npm 不升级锁文件版本（`npm ci`） |
